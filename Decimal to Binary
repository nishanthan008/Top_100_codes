#include <stdio.h>
int main() {
  int n,rev,i=0,b[10];
  printf("enter the Decimal number:");
  scanf("%d",&n);
  while(n>0)
  {
      b[i++]=n%2;
      n=n/2;
  }
    for(int j=i-1;j>=0;j--)
    {
        printf("%d",b[j]);
    }
    return 0;
}
