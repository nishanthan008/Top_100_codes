#include <stdio.h>
#include<math.h>
int main() {
  int n,rev,i=0,sum=0;
  printf("enter the binary number:");
  scanf("%d",&n);
  while(n>0)
  {
      rev=n%10;
      sum=sum+(rev*pow(2,i));
      n=n/10;
      i++;
  }
  printf("Decimal number is %d",sum);
    return 0;
}
