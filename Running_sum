#include <iostream>
#include<vector>
using namespace std;
int main()
{
    vector<int>arr={2,3,4,8,1};
    int n=arr.size(),sum=arr[0];
    for(int i=1;i<n;i++)
    {
        sum+=arr[i];
        arr[i]=sum;
    }
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }
    return 0;
}
