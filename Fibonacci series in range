#include <stdio.h>
int main() {
  int n1=0,n2=1,n3=0,s,e;
  printf("enter the value:");
  scanf("%d",&s);
    printf("enter the value:");
    scanf("%d",&e);
  for(int i=1;n3<e;i++)
  {
      n3=n1+n2;
      if(s<=n3) {
          printf("%d\n", n3);
      }
      n1=n2;
      n2=n3;
  }
    return 0;
}
